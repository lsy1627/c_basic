# 问题总结

## 一、最大公共子串 `Find_Max_Pub`

**题目**：找出两个字符串中最长的公共子串。输入 `abcdefgxxxxxx` 和 `xxxxxabcdefgdeded`，应输出 `abcdefg`。

### 问题 1：保存了错位的指针

```c
if(len > max_len){
    max_len = len;
    Max_Pub = a;    // 错：a 已越过匹配段末尾
}
```

`a` 内层循环结束后早已越过匹配段的最后一个字符，指向**第一个不匹配的字符**（或 `\0`）。应改 `Max_Pub = p;` 保存匹配起点。

### 问题 2：`%s` 会打印匹配段之后的所有内容

就算把指针改成 `p`，`printf("%s", max)` 也会把从 `p` 到字符串结尾的**全部字符**打出来。因为 `%s` 不知道"匹配了多长"，只能打到 `\0` 为止。必须限制长度：`printf("%.*s", len, p)` 或循环 `printf("%c", ...)`。

### 问题 3：长度没传出来

```c
int max_len = 0;   // main 里自己声明了一个，永远不会被函数里的改变
char* max = Find_Max_Pub(str1, str2);  // 返回的只有指针
```

函数里的 `max_len` 是局部变量，同名变量不会同步。要么用指针参数带出，要么在函数内拷贝子串补 `\0` 再返回。

### 问题 4：打印差一错误

```c
for(int i = 0; i <= max_len; i++)  // 注意是 <=，会多打一个
```

`i` 从 0 到 `max_len` 共 `max_len + 1` 次。所以得到 `abcdefgx`（8 个）而不是 `abcdefg`（7 个）。

### 问题 5：`printf` 格式符与类型不匹配

```c
printf("%s", max[i]);   // max[i] 是 char，%s 需要 char*
```

应改为 `printf("%c", max[i]);`

### 问题 6：`len` 初始值混乱

`int len = 1;` 与 while 递增配合，恰好抵消（匹配 L 个字符时 len = L）。虽然结果碰巧对，但语义不清，标准写法是 `int len = 0;` 在循环里 `len++`。

---

## 二、字符串整体倒置的交换问题

**核心知识点**：为什么交换必须用临时变量。

```c
*start = *end;    // 原 start 的值被覆盖丢失
*end = *start;    // 写进去的是已经变过的值
```

没有 `temp`，两个位置都会变成同一个值，另一个字符丢失。必须：

```c
char temp = *start;
*start = *end;
*end = temp;
```

类比：两个杯子换水必须有第三个空杯子。

---

## 三、句子倒置 `Inver_Str`

**题目**：`I am from shanghai` -> `shanghai from am I`。思路：整体倒置 -> 再逐单词倒置。

### 问题 1：内部循环条件用错指针 -> 死循环/越界

```c
char *word_start = p;
while(*word_start != '\0' && *word_start != ' ') p++;   // 错
```

`word_start` 被固定在单词起点（字母），它的值永远不会变成空格或 `\0`，`p++` 会无限跑出字符串。

应判断**正在移动的** `p`：

```c
while(*p != '\0' && *p != ' ') p++;
```

### 问题 2：`char *end = src;` 放在循环体内

```c
while(*p != '\0'){
    char *end = src;   // 每轮重置，永远指回开头
    ...
}
```

需要累积推进的指针必须定义在循环外面。

### 问题 3：注释里的倒置结果写错

代码注释写"此时字符串变为 `I ma morf iahgnahs`"，实际整体倒置应是 `iahgnahs morf ma I`。

### 边界情况

- 空字符串：`end--` 后 start > end 安全跳过。
- 单个单词 `hello`：倒过去再倒回来，结果正确。
- 首尾/多个连续空格：`while(*p == ' ')` 会连续跳过，逻辑依然正确。

---

## 四、`#define` 错误使用

### 问题 1：`#define` 不能声明变量

```c
#define char h[128]  head
#define char t[128]  tail
```

`#define` 是纯文本替换，不能用来声明变量。预处理后文件里**每一处** `char` 都被替换成 `h[128] head`，全文件编译崩溃。正确写法是直接 `char h[128];`。

### 问题 2：指针按值传递

```c
char *h = "head";
while(*h != '\0') h++;   // 移动的是局部指针，数组本身没变
```

函数里移动指针不影响调用者；如果是数组名 `h`，`h++` 甚至连编译都不通过（数组名不是左值）。

---

## 五、头尾拼接 `add_h_t`

**题目**：head + 输入 + tail 拼接。

### 问题 1：往字符串常量里写 -> 崩溃

```c
const char *h = "head";
while(*h != '\0') h++;   // h 指向 "head" 的 '\0'
*h = *end;               // 写只读内存 -> 段错误/未定义行为
```

`"head"` 是字符串字面量，通常存在于只读内存，且只有 5 字节，不能当目标缓冲区。

### 问题 2：`const` 导致编译错误 C137（"必须是可修改的左值"）

```c
const char *h;   // 只读指针
*h = ...;        // 编译报错：写入只读目标
```

规则：**读用 `const char *`，写用 `char *`，两者分开**。

### 问题 3：缺 `#include <stdlib.h>`

用 `malloc` 却没引头文件，部分编译器直接编译失败 -> 没有生成 .exe。

### 问题 4：文件保存时忘了 `.c` 扩展名

编译任务按 `train5_add_h_t.c` 找文件找不到 -> 编译失败 -> PowerShell 报"无法将 `....exe` 识别为 cmdlet"。**"exe 找不到"的报错，先怀疑编译没成功，再看代码逻辑。**

### 问题 5：内存泄漏

函数内 `malloc` 的结果必须 `free`，且别把函数返回的起点指针弄丢。

---

## 六、查找只出现一次的字符 `only`

**题目**：输入 `aabccddeeff` 应输出 `b`。

### 问题 1：返回指针指向缓冲区末尾

```c
char *end = (char*)malloc(128);
...
return end;    // end 在最后一次写入之后
```

`printf("%s", end)` 从末尾开始打印**未初始化的垃圾内存** -> 输出乱码 `P圫`。应返回 `buf`（开头）。

### 问题 2：没写 `'\0'` 结束符

`%s` 没有结束符就停不下来，一直读到垃圾。

### 问题 3：判断逻辑"边扫边决定"

```c
int count = 0;                 // 没在每次外部循环清零
for(每个 q){
    if(*p == *q) count = 1;
    if(count == 0){ 写入; }    // 还没扫完就急着写
}
```

"只出现一次"必须**先完整数一遍**再判断，不能在扫描途中下结论。

### 问题 4：条件写错

```c
if(count == 0)   // 每个字符至少命中自己一次，count 最小也是 1，永远不成立
```

应改为 `if(count == 1)`，表示"整个字符串里这个字符只出现了一次"。

### 问题 5：内存管理

- `malloc` 起点指针弄丢 -> 泄漏且无法正确打印。
- main 里只 `free(str)`，漏了 `free(s2)`。
- 多余的未使用变量 `char *out = src;` `char *start = src;`。

---

## 七、Git 提交遇到的问题（train4/train5）

1. 推送被拒（远程有别人清理 .exe 的提交）-> 需要先 `fetch`。
2. 用 `git pull --rebase --autostash` 解决；autostash 恢复本地修改时与远程删除的 exe 冲突。
3. 冲突文件全是构建产物（.exe），按"保留远程删除"处理（`git rm --cached`）。
4. 用户真实的源码改动 `train3_ju_pub.c` 保留在工作区未提交，未被误删。

---

## 八、通用自查清单

| 类别 | 自查清单 |
|------|---------|
| **指针返回** | 返回的是"起点"还是"游标"？起点才能被打印/释放 |
| **指针读写** | 读用 `const char*`，写用 `char*`，`*p =` 时 p 必须可写 |
| **循环变量** | 条件里判断的指针和 `++` 的指针是不是同一个？ |
| **变量作用域** | 需要累计的放循环外；需要每轮清零的放循环内 |
| **字符串结束** | 写入后一定补 `'\0'`；`%s` 没有它停不下来 |
| **长度限制** | `for` 边界是 `<`；长度从函数里"传出来"，不要 main 里自造 |
| **交换** | 涉及两个变量的调换必须有第三个临时变量 |
| **编译 vs 运行** | 报错先确认编译是否成功（头文件/语法/文件扩展名），再查逻辑 |
| **内存** | malloc 配 free，且释放的必须是 malloc 返回的原始指针 |
| **注释** | 注释要和实际行为一致，否则会误导排查 |